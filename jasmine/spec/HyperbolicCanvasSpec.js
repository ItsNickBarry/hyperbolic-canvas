describe('HyperbolicCanvas', function () {
  it('should define the natural and just constant tau', function () {
    expect(Math.TAU).toEqual(Math.PI * 2);
  });

  // TODO HyperbolicCanvas.create
});
